spring:
  datasource:
    url: jdbc:mysql://localhost:3306/simple_board?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: app
    password: app1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
jwt:
  secret: "change-this-to-a-long-secret-string-at-least-32-chars"
  access-token-exp-min: 60